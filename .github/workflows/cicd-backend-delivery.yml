name: CI/CD - Backend Delivery-Service

on:
  push:
    branches: [deploy]
    paths:
      - 'delivery-service/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Obtener version
        id: version
        run: echo "VERSION=v$(date +%Y%m%d%H%M%S)" >> $GITHUB_OUTPUT

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build y Push imagen
        run: |
          docker build -t dockermasterluka/delivery-services:${{ steps.version.outputs.VERSION }} ./delivery-service
          docker push dockermasterluka/delivery-services:${{ steps.version.outputs.VERSION }}