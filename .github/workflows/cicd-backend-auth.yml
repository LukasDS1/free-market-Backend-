name: CI/CD - Backend Auth-Service

on:
  push:
    branches: [deploy]
    paths:
      - 'auth-service/**'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Obtener version
        id: version
        run: echo "VERSION=v$(date +%Y%m%d%H%M%S)" >> $GITHUB_OUTPUT

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build y Push imagen
        run: |
          docker build -t dockermasterluka/auth-services:${{ steps.version.outputs.VERSION }} ./auth-service
          docker push dockermasterluka/auth-services:${{ steps.version.outputs.VERSION }}